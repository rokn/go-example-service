app:
  port: "8080"

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "example"
  sslmode: "disable"

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

auth:
  secret_key: "your-production-secret-key-here"  # Change this in production!
  realm: "redis example api"
  timeout_hours: 24
  max_refresh_hours: 24 